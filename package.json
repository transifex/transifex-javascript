{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap",
    "test": "lerna run test",
    "build": "lerna run build",
    "release": "lerna version",
    "publish": "lerna exec -- npm publish"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
